rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access across all collections for Coach Boda Platform
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
